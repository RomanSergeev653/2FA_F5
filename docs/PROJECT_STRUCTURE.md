# 📁 Структура проекта

## Обзор

Проект организован по модульному принципу для удобства разработки и поддержки.

## Директории

### Корневая директория

- `main.py` - Точка входа приложения, инициализация бота
- `config.py` - Конфигурация: настройки бота, IMAP, лимиты
- `requirements.txt` - Python зависимости
- `Dockerfile` - Docker образ для контейнеризации
- `docker-compose.yml` - Docker Compose конфигурация
- `README.md` - Основная документация проекта

### `database/` - Работа с базой данных

- `models.py` - Модели данных и инициализация SQLite БД
- `db_manager.py` - Менеджер БД: CRUD операции, логирование

**Таблицы:**
- `users` - Пользователи бота
- `permissions` - Разрешения доступа
- `action_logs` - Логи действий

### `handlers/` - Обработчики команд Telegram

- `start.py` - Команды: `/start`, `/help`, `/menu`, `/tips`
- `registration.py` - Регистрация: `/register`, `/unregister`
- `permissions.py` - Разрешения: `/request_access`, `/my_permissions`, `/revoke`, `/pending_requests`
- `codes.py` - Получение кодов: `/get_code`, `/my_code`, `/check_email`

### `utils/` - Утилиты

- `keyboards.py` - Генерация inline клавиатур для Telegram
- `messages.py` - Шаблоны и форматирование сообщений
- `security.py` - Безопасность: валидация, rate limiting, санитизация
- `email_parser.py` - Парсинг почты через IMAP, поиск 2FA кодов
- `encryption.py` - Шифрование/расшифрование паролей (Fernet)

### `docs/` - Документация

- `PLAN.md` - План улучшений и TODO
- `SECURITY_FIXES.md` - Документация по исправлениям уязвимостей
- `PROJECT_STRUCTURE.md` - Этот файл

### `docker/` - Docker скрипты

- `deploy.sh` - Скрипт автоматического деплоя на сервер

### `.github/workflows/` - CI/CD

- `deploy.yml` - GitHub Actions workflow для автоматического деплоя

### `logs/` - Логи

- Директория для логов приложения (не в git)

## Принципы организации

1. **Разделение ответственности** - каждый модуль отвечает за свою область
2. **Переиспользование** - общие функции вынесены в `utils/`
3. **Безопасность** - все проверки безопасности в `utils/security.py`
4. **Документация** - вся документация в `docs/`

## Добавление новых функций

1. **Новая команда** - добавить обработчик в соответствующий файл в `handlers/`
2. **Новая утилита** - добавить в `utils/`
3. **Новая таблица БД** - добавить в `database/models.py` и методы в `db_manager.py`
4. **Новая клавиатура** - добавить функцию в `utils/keyboards.py`
