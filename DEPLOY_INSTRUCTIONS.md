# 📋 Инструкции по настройке автоматического деплоя

## Вариант 1: Настройка через SSH ключ (Рекомендуется)

### Шаг 1: Генерация SSH ключа на локальной машине

Если у вас ещё нет SSH ключа, создайте его:

**Для Windows (cmd):**
```cmd
# Сначала создайте директорию (если её нет)
mkdir %USERPROFILE%\.ssh

# Затем создайте ключ
ssh-keygen -t ed25519 -C "github-actions-deploy" -f %USERPROFILE%\.ssh\github_deploy
```

Или если ed25519 не поддерживается:
```cmd
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f %USERPROFILE%\.ssh\github_deploy
```

**Для Linux/Mac:**
```bash
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy
```

Или если ed25519 не поддерживается:
```bash
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f ~/.ssh/github_deploy
```

**Важно:** Не устанавливайте пароль на ключ (просто нажмите Enter дважды).

### Шаг 2: Копирование публичного ключа на сервер

Скопируйте публичный ключ на сервер:

**Для Windows (cmd) - стандартный порт 22:**
```cmd
type %USERPROFILE%\.ssh\github_deploy.pub | ssh USER@SERVER_IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Для Windows (cmd) - нестандартный порт (например, 653):**
```cmd
type %USERPROFILE%\.ssh\github_deploy.pub | ssh -p 653 USER@SERVER_IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Для Linux/Mac - стандартный порт 22:**
```bash
cat ~/.ssh/github_deploy.pub | ssh USER@SERVER_IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Для Linux/Mac - нестандартный порт (например, 653):**
```bash
cat ~/.ssh/github_deploy.pub | ssh -p 653 USER@SERVER_IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Или вручную:**
1. Откройте файл `~/.ssh/github_deploy.pub` (или `C:\Users\Admin\.ssh\github_deploy.pub` на Windows)
2. Скопируйте его содержимое
3. Подключитесь к серверу:
   - Стандартный порт: `ssh USER@SERVER_IP`
   - Нестандартный порт: `ssh -p 653 USER@SERVER_IP` (замените 653 на ваш порт)
4. Выполните:
   ```bash
   mkdir -p ~/.ssh
   echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

### Шаг 3: Проверка подключения

Проверьте, что подключение работает:

**Стандартный порт (22):**
```bash
# Windows (cmd)
ssh -i %USERPROFILE%\.ssh\github_deploy USER@SERVER_IP

# Linux/Mac
ssh -i ~/.ssh/github_deploy USER@SERVER_IP
```

**Нестандартный порт (например, 653):**
```bash
# Windows (cmd)
ssh -i %USERPROFILE%\.ssh\github_deploy -p 653 USER@SERVER_IP

# Linux/Mac
ssh -i ~/.ssh/github_deploy -p 653 USER@SERVER_IP
```

### Шаг 4: Добавление секретов в GitHub

#### Пошаговая инструкция:

1. **Откройте ваш репозиторий на GitHub:**
   - Перейдите по адресу: `https://github.com/RomanSergeev653/2FA_F5`
   - В верхней части страницы нажмите на вкладку **Settings** (Настройки)

2. **Откройте раздел секретов:**
   - В левом меню найдите и нажмите **Secrets and variables**
   - Выберите **Actions**

3. **Добавьте каждый секрет по очереди:**
   - Нажмите кнопку **New repository secret** (зелёная кнопка справа)
   - Для каждого секрета заполните:
     - **Name** (Имя) - название секрета
     - **Secret** (Значение) - значение секрета
   - Нажмите **Add secret**

#### Список секретов для добавления:

**1. `SERVER_HOST`**
   - **Name:** `SERVER_HOST`
   - **Secret:** IP адрес или домен вашего сервера
   - **Пример:** `202.148.55.233` или `example.com`
   - Просто скопируйте IP адрес вашего сервера

**2. `SERVER_USER`**
   - **Name:** `SERVER_USER`
   - **Secret:** имя пользователя для SSH
   - **Пример:** `root` или `deploy`
   - Это имя пользователя, которое вы используете для подключения к серверу

**3. `c`** ⚠️ **Важно!**
   - **Name:** `SERVER_SSH_KEY`
   - **Secret:** **весь содержимый файл приватного ключа**
   - **Как получить:**
     
     **Windows (cmd):**
     ```cmd
     type %USERPROFILE%\.ssh\github_deploy
     ```
     Скопируйте **всё содержимое** (начинается с `-----BEGIN OPENSSH PRIVATE KEY-----` или `-----BEGIN RSA PRIVATE KEY-----` и заканчивается `-----END ... KEY-----`)
     
     **Или откройте файл в блокноте:**
     ```cmd
     notepad %USERPROFILE%\.ssh\github_deploy
     ```
     Выделите всё (Ctrl+A), скопируйте (Ctrl+C) и вставьте в поле Secret
     
     **Linux/Mac:**
     ```bash
     cat ~/.ssh/github_deploy
     ```
     Скопируйте всё содержимое
   
   - **Пример содержимого:**
     ```
     -----BEGIN OPENSSH PRIVATE KEY-----
     b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAACFwAAAAdzc2gtcn
     ... (много строк) ...
     -----END OPENSSH PRIVATE KEY-----
     ```
   - ⚠️ **Вставляйте ВЕСЬ файл целиком, включая строки с BEGIN и END!**

**4. `SERVER_PORT`**
   - **Name:** `SERVER_PORT`
   - **Secret:** номер порта SSH
   - **Пример:** `653` (если у вас нестандартный порт) или `22` (если стандартный)
   - ⚠️ **Обязательно укажите, если используете нестандартный порт!**

**5. `SERVER_DEPLOY_PATH`**
   - **Name:** `SERVER_DEPLOY_PATH`
   - **Secret:** полный путь к проекту на сервере
   - **Пример:** `/var/www/2FA_F5` или `/home/user/2FA_F5` или `/root/2FA_F5`
   - Это путь, где находится ваш проект на сервере (куда вы клонировали репозиторий)

#### Визуальная подсказка:

После добавления всех секретов, в разделе **Secrets** вы должны увидеть список:
- ✅ `SERVER_HOST`
- ✅ `SERVER_USER`
- ✅ `SERVER_SSH_KEY`
- ✅ `SERVER_PORT`
- ✅ `SERVER_DEPLOY_PATH`

**Важно:** После добавления секрета, его значение больше нельзя просмотреть (только обновить или удалить). Это сделано для безопасности.

### Шаг 5: Подготовка сервера

На сервере выполните:

```bash
# Перейдите в директорию проекта
cd /path/to/your/project

# Убедитесь, что репозиторий настроен
git remote -v

# Если нужно, настройте git
git config --global user.name "Deploy Bot"
git config --global user.email "deploy@example.com"

# Убедитесь, что docker-compose установлен
docker-compose --version

# Сделайте скрипт деплоя исполняемым (если используете)
chmod +x deploy.sh
```

---

## Вариант 2: Настройка через логин/пароль (Менее безопасно)

Если вы предпочитаете использовать логин/пароль вместо SSH ключа, нужно изменить workflow файл.

### Изменения в `.github/workflows/deploy.yml`:

Замените секцию `Deploy to server via SSH` на:

```yaml
- name: Deploy to server via SSH (with password)
  uses: appleboy/ssh-action@v1.0.3
  with:
    host: ${{ secrets.SERVER_HOST }}
    username: ${{ secrets.SERVER_USER }}
    password: ${{ secrets.SERVER_PASSWORD }}
    port: ${{ secrets.SERVER_PORT || 22 }}
    script: |
      cd ${{ secrets.SERVER_DEPLOY_PATH }}
      git pull origin master
      docker-compose down
      docker-compose pull
      docker-compose up -d --build
      docker-compose ps
      echo "✅ Деплой завершён успешно!"
```

### Секреты для GitHub:

- **`SERVER_HOST`** - IP адрес сервера
- **`SERVER_USER`** - имя пользователя
- **`SERVER_PASSWORD`** - пароль пользователя
- **`SERVER_PORT`** - порт SSH (опционально, по умолчанию 22, но **обязательно укажите, если используете нестандартный порт**, например `653`)
- **`SERVER_DEPLOY_PATH`** - путь к проекту на сервере

---

## Проверка работы

После настройки:

1. Сделайте любой коммит и запушьте в `master`:
   ```bash
   git add .
   git commit -m "Test deploy"
   git push origin master
   ```

2. Перейдите в GitHub → **Actions** и посмотрите выполнение workflow

3. На сервере проверьте логи:
   ```bash
   docker-compose logs -f
   ```

---

## Устранение проблем

### Ошибка: "Ключ был создан на сервере вместо локальной машины"

Если вы случайно создали SSH ключ на сервере, удалите его:

```bash
# На сервере выполните:
rm ~/.ssh/github_deploy
rm ~/.ssh/github_deploy.pub
```

Затем создайте ключ правильно на **локальной машине** (см. Шаг 1).

### Ошибка: "Permission denied (publickey)"

- Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте права доступа: `chmod 600 ~/.ssh/authorized_keys`

### Ошибка: "Connection refused" или "ssh: connect to host ... port 22"

Если вы используете нестандартный порт SSH (не 22), обязательно укажите его:
- В командах SSH используйте флаг `-p ПОРТ` (например: `ssh -p 653 USER@SERVER_IP`)
- В GitHub Secrets обязательно добавьте `SERVER_PORT` с вашим номером порта (например: `653`)

### Ошибка: "git pull requires authentication"

На сервере настройте git для работы без пароля:
```bash
git config --global credential.helper store
# Или используйте SSH URL для remote:
git remote set-url origin git@github.com:RomanSergeev653/2FA_F5.git
```

### Ошибка: "docker-compose: command not found"

Установите docker-compose на сервере или используйте `docker compose` (новая версия):
```bash
# В workflow замените docker-compose на docker compose
```

---

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте секреты в репозиторий
- Используйте SSH ключи вместо паролей когда возможно
- Регулярно обновляйте зависимости
- Ограничьте доступ к серверу только необходимыми IP адресами
